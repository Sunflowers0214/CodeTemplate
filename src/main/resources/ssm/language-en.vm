##首字母大写
#macro(firstToUpper $field)$field.toUpperCase().substring(0,1)$field.substring(1)#end
##首字母小写
#macro(firstToLower $field)$field.toLowerCase().substring(0,1)$field.substring(1)#end
var #firstToLower($tableInfo.tableLabel)DataName=[{
#set( $count = 1)
#foreach($field in $tableInfo.noteshuColList)
	#firstToLower($field.columnLabel):"$field.columnLabel"#if($velocityCount!=${tableInfo.noteshuColList.size()}),
#end
#set($count=$count+1)
#end

}];

var #firstToLower($tableInfo.tableLabel)GridHead=[
#foreach($field in $tableInfo.noteshuColList)
#if(${field.columnSimpleClassName.toLowerCase()}=="date")
	{display: '$field.columnLabel', name: '#firstToLower($field.columnLabel)', width: '10%', align: 'center', isSort: false,
		render:function (rowdata, rowindex, value) {
			if(value&&value.length>10){
				return value.substring(0,10);
			}else{
				return "";
			}
		}
	},
#else
	{display: '$field.columnLabel', name: '#firstToLower($field.columnLabel)', width: '10%', align: 'center', isSort: false},
#end
#end
	{display: 'operation', isSort: false, width: 90, 
		render: function (rowdata, rowindex, value){
			var optGrp;
#foreach($field in $tableInfo.columnList)
#if(${field.isPrimaryKey()})
			optGrp = [
				{"name":"Modify","icon":"fa-pencil","action":"init#firstToUpper($tableInfo.tableLabel)Modify('" + rowdata.#firstToLower($field.columnLabel) + "')"},
			  	{"name":"Delete","icon":"fa-trash-o","action":"init#firstToUpper($tableInfo.tableLabel)Delete('" + rowdata.#firstToLower($field.columnLabel) + "')"}
			];
#end
#end
    	    return outputOpt(optGrp);
		}
	}
];
	                
var ADD = "add";
var DELETE = "delete";
var MODIFY = "modify";
var DETAIL = "detail";
var gsearchWord = "search";
var gsaveWord = "save";
var gcloseWord = "close";
var #firstToLower($tableInfo.tableLabel)Management="${tableInfo.tabComment}";
var #firstToLower($tableInfo.tableLabel)ManagementADD=#firstToLower($tableInfo.tableLabel)Management+ADD;
var #firstToLower($tableInfo.tableLabel)ManagementMODIFY=#firstToLower($tableInfo.tableLabel)Management+MODIFY;
var #firstToLower($tableInfo.tableLabel)ManagementDETAIL=#firstToLower($tableInfo.tableLabel)Management+DETAIL;
var #firstToLower($tableInfo.tableLabel)DelMsg="Are you sure you want to delete ${tableInfo.tabComment}？";